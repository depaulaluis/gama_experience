import React, {Component} from 'react';

class Entrada extends Component {
    render(){
        return(
        <span>Entrada</span>
        <input type="text" onChange-(this.pro) />
        <button>Procurar</button>

        )
    }
}

export default Entrada;